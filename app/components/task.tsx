export default function Task({
    // params,
  }: {
    // params: 
  }) {
    return (
    <div className="">
        <div className="header">
          <h1>{params.name}</h1>
        <p>{params.status}</p>
        </div>
        </div>
)
  }